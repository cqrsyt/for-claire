/**
 * Site base path — change this when deploying:
 *   "" or "./"     → Cloudflare Pages / custom domain at root
 *   "/for-claire/" → GitHub Pages project site
 */
window.SITE_CONFIG = {
  basePath: "", // default: root (Cloudflare / custom domain)
};
